// Specify the tasks to use when you run 'gradle'
defaultTasks 'clean', 'build'

// Apply the magma.gradle file
apply from: 'magma.gradle'

// Basic project information
group = 'com.example'
archivesBaseName = 'exampleaddon'
version = '1.0.0-SNAPSHOT'
